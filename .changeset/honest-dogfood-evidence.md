---
"executable-trust-layer": minor
---

Prefer repository-owned check or verify gates, prevent inferred checks from claiming user-visible
behavior, keep generated guided artifacts out of rerun change sets, format generated YAML before
repository gates, deduplicate Playwright discovery, recognize ADR and nested-repository boundaries,
and classify denied local socket binding as insufficient infrastructure evidence.
