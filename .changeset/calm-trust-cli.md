---
"executable-trust-layer": minor
---

Make `trust --intent "<outcome>"` the primary verification command and keep the default help focused
on the four everyday workflows. The existing expert commands remain available through
`trust --help --all` for backward compatibility and advanced automation.
