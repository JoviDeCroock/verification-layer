version: 1
id: guided-verification-uses-canonical-project-gates-without-overstating-inf
intent: Guided verification uses canonical project gates without overstating inferred behavior,
  excludes generated artifacts on reruns, improves discovery accuracy, and classifies denied local
  sockets as insufficient evidence
expected_behaviors:
  - id: primary-behavior
    description:
      Guided verification uses canonical project gates without overstating inferred behavior,
      excludes generated artifacts on reruns, improves discovery accuracy, and classifies denied
      local sockets as insufficient evidence
    evidence:
      - test
    evidence_mapping: explicit
affected_surfaces:
  - repository
risks: []
required_evidence:
  - typecheck
  - lint
  - test
  - build
qa_missions:
  - id: primary-behavior
    title: Guided verification uses canonical project gates without overstating inferred behavior,
      excludes generated artifacts on reruns, improves discovery accuracy, and classifies denied
      local sockets as insufficient evidence
    objective:
      Guided verification uses canonical project gates without overstating inferred behavior,
      excludes generated artifacts on reruns, improves discovery accuracy, and classifies denied
      local sockets as insufficient evidence
    derived_from:
      - Guided verification uses canonical project gates without overstating inferred behavior,
        excludes generated artifacts on reruns, improves discovery accuracy, and classifies denied
        local sockets as insufficient evidence
    viewport: desktop
    generation:
      method: deterministic
      generator: executable-trust-layer/intent-heuristics
      version: "1"
      input_sha256: b94202664b983a4eae34c99c77ddc4edc7ca1f2d5211d94dae543e4b1742eefb
excluded: []
approval:
  status: approved
  approved_by: local-user
  approved_at: 2026-08-27T07:06:07.429Z
  content_sha256: 0aaae0f1f26df3eb15822e9eb8b19eec2449672378d346d7c92fecba693b9a48
  method: local
