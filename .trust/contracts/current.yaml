version: 1
id: move-all-internal-implementation-modules-into-one-root-src-tree-while-pr
intent: Move all internal implementation modules into one root src tree while preserving the trust
  CLI and package behavior
expected_behaviors:
  - id: primary-behavior
    description: Move all internal implementation modules into one root src tree while preserving the
      trust CLI and package behavior
    evidence:
      - typecheck
      - lint
      - test
      - build
affected_surfaces:
  - repository
risks: []
required_evidence:
  - typecheck
  - lint
  - test
  - build
qa_missions:
  - id: primary-behavior
    title: Move all internal implementation modules into one root src tree while preserving the trust
      CLI and package behavior
    objective: Move all internal implementation modules into one root src tree while preserving the
      trust CLI and package behavior
    derived_from:
      - Move all internal implementation modules into one root src tree while preserving the trust
        CLI and package behavior
    viewport: desktop
    generation:
      method: deterministic
      generator: executable-trust-layer/intent-heuristics
      version: "1"
      input_sha256: 08c44b4db0e2884c9645610e725483cbaa5e1b4c520e17bfb4f5e0418153c4d2
excluded: []
approval:
  status: approved
  approved_by: local-user
  approved_at: 2026-08-26T18:12:25.722Z
  content_sha256: 9aa76694722c5b0c1976f3774a6807fc63e12d497d63561abc15563a79b01248
  method: local
